README

Name: Henry Lo and Chris Tzanidis
Section: LBL B2 (85872)

Coding instructions:
Using bash, run command "ipython3 -i assignment1.py" to start testing code through interactive shell.
If __name__ is defined as the string __main__, runs built-in test code.
If not defined, builds a graph based on edmonton map txt and asks for input based off of the 'server' code.
